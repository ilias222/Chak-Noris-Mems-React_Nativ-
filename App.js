import { StatusBar } from 'expo-status-bar';
import { Button, StyleSheet, Text, View, TextInput } from 'react-native';
import { Navigate } from './screens/routes/Navigate';


export default function App() {
  return (
    // <View style={styles.container}>
    <Navigate />
  );
}
